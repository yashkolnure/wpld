// All prices in paise (₹1 = 100 paise)
// Based on Meta India rates + 25% markup

export const PRICING = {
  marketing: 90,      // ₹0.90/msg — broadcast/bulk/template campaigns
  service:   20,      // ₹0.20/msg — messages within 24hr window
};

export const META_BASE = {
  marketing: 72,      // ₹0.72 Meta base rate
  service:   16,      // ₹0.16 Meta base rate
};

export const MARKUP_PCT = 25;
